/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { PlusPipe } from './plus.pipe';

describe('Pipe: Pluse', () => {
  it('create an instance', () => {
    let pipe = new PlusPipe();
    expect(pipe).toBeTruthy();
  });
});
